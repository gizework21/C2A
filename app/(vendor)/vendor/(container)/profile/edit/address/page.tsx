import { AddressProfileForm } from "@/components/vendor";
import React, { Suspense } from "react";

const Page = () => {
  return (
    <Suspense>
      <AddressProfileForm />
    </Suspense>
  );
};

export default Page;
